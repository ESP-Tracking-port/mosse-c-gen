PYINTERP="@python@"

out:
	$(PYINTERP) main.py

testcompile: out
	cp stub/CMakeLists.txt out
	cp stub/main.cpp out
	cd out && mkdir -p build && cd build && cmake .. && cmake --build .

clean:
	rm -rf out/ autom4te.cache/
	rm -f config.log config.status

.PHONY: testcompile clean
